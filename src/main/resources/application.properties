#Embedded server
server.port=8080

#context path for web app while running it as standlone app
server.servlet.context-path=/FirstBootMVCApp

# view resolver input (prefix, suffix) for default InternalResourceViewResolver
spring.mvc.view.prefix=/WEB-INF/pages/
spring.mvc.view.suffix=.jsp
